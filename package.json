{
  "main": "index.js",
  "scripts": {
    "build": "node_modules/.bin/esbuild client/src/index.js client/src/style.css --bundle --outdir=build/resources/main/public --sourcemap",
    "build:watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "esbuild": "0.20.1"
  },
  "dependencies": {
    "htmx.org": "^2.0.0",
    "purecss": "^3.0.0"
  }
}
